/*
int CutNumber(int number)
{
    int sot = number / 100;
    int edi = number % 10;
    int result = sot*10 + edi; 
    return result;
}


int randNumber = new Random().Next(100, 1000);
int NewNumber = CutNumber(randNumber);
Console.WriteLine($"new version of a number {randNumber} is {NewNumber}" );
*/
